# =============================================================================
# NJ Plumbing — Cloud Functions secrets (server-side only)
# =============================================================================
# Copy to:  functions/.env.nj-plumbing   (Firebase project nj-plumbing)
#            OR functions/.env            (generic local name)
#
# Windows PowerShell:
#   copy functions\.env.example functions\.env.nj-plumbing
#   notepad functions\.env.nj-plumbing
#
# Never use a VITE_ prefix here — browser bundles must not contain these secrets.
# Never commit the real file — functions/.env* is gitignored (except this example).
# =============================================================================

# --- OpenAI (Plaud call analysis + work-order PDF extraction) ---
OPENAI_API_KEY=sk-replace-with-your-openai-api-key
OPENAI_MODEL=gpt-5.6-sol

# --- Google Maps (production Map tab — also used at hosting build time) ---
# Same key as VITE_GOOGLE_MAPS_API_KEY in .env.local, or create a separate
# server-restricted key. Enable Maps JavaScript, Geocoding, Directions APIs.
GOOGLE_MAPS_API_KEY=AIzaSy-replace-with-your-maps-key

# --- Plaud call intake ---
# After `plaud login`, copy refresh_token from %USERPROFILE%\.plaud\tokens.json
PLAUD_REFRESH_TOKEN=replace-with-plaud-refresh-token
PLAUD_ACCESS_TOKEN=
PLAUD_API_BASE=https://platform.plaud.ai/developer/api

# --- Microsoft Graph (Teams channel PDF import) — optional ---
MICROSOFT_TENANT_ID=00000000-0000-0000-0000-000000000000

# --- Twilio (SMS + voice confirmations) ---
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=replace-with-your-twilio-auth-token
TWILIO_PHONE_NUMBER=+15555551212
SMS_TEST_RECIPIENT=+18609643025
COMPANY_NAME=NJ Plumbing

# --- Dispatch / reminders ---
DISPATCH_ORIGIN_ADDRESS=216 Christian Lane, Berlin, CT
DISPATCH_MORNING_HOUR=7
BUSINESS_TIME_ZONE=America/New_York
MORNING_REMINDER_HOUR=8

# --- Gmail voice-email ingestion (optional legacy path) ---
GMAIL_EMAIL=your-inbox@example.com
GMAIL_CLIENT_ID=replace-with-google-oauth-client-id.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=replace-with-google-oauth-client-secret
GMAIL_REFRESH_TOKEN=replace-with-google-oauth-refresh-token
GMAIL_REDIRECT_URI=http://localhost

# --- Gemini (optional; voice email path only) ---
GEMINI_API_KEY=replace-with-gemini-api-key-if-using-voice-email
